# This is a Databricks asset bundle definition for Youtube_Project_Bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Youtube_Project_Bundle
  uuid: 4ff79d2d-5998-46f3-984d-fc8f0713f893

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-60e65b1b-fea1.cloud.databricks.com
    variables:
      catalog: workspace
      schema: dev
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-60e65b1b-fea1.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/vinay.kale9@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/vinay.kale9@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: vinay.kale9@gmail.com
        level: CAN_MANAGE
